# Compiled output and build artifacts
out/          # IntelliJ IDEA's default output directory
build/        # Gradle's default build output directory
.gradle/      # Gradle's cache and generated files

# IDE specific files
.idea/         # IntelliJ IDEA project files (excluding shared run configurations)
*.iml          # IntelliJ IDEA module files
*.iws          # IntelliJ IDEA workspace files
*.ipr          # IntelliJ IDEA project files (older format)

# Kotlin specific temporary files
*.kpt
*.ksd

# Gradle Wrapper JAR (optional: uncomment '!' if you want to commit the wrapper JAR)
# The wrapper script itself (`gradlew`, `gradlew.bat`) should *always* be committed.
gradle/wrapper/*.jar
#!gradle/wrapper/gradle-wrapper.jar # Uncomment this line if you specifically want to track the JAR

# Operating System specific files
.DS_Store      # macOS folder metadata
Thumbs.db      # Windows thumbnail cache
ehthumbs.db    # Windows thumbnail cache (hidden)
Desktop.ini    # Windows custom folder settings

# Log files
*.log